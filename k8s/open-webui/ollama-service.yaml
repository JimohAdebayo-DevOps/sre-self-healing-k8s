apiVersion: v1
kind: Service
metadata:
  name: ollama-service
  namespace: open-webui
spec:
  type: ClusterIP
  ports:
  - port: 11434
    targetPort: 11434
    protocol: TCP
    name: api
  selector:
    app: ollama
