apiVersion: v1
kind: Service
metadata:
  name: mosquitto-service
  namespace: gridops-edge
spec:
  type: LoadBalancer
  selector:
    app: mosquitto
  ports:
    - protocol: TCP
      port: 1883
      targetPort: 1883
