# sre-self-healing-k8s/k8s/cilium/values.yaml
kubeProxyReplacement: strict
k8sServiceHost: "YOUR_MASTER_NODE_IP" 
k8sServicePort: "6443"

encryption:
  enabled: true
  type: wireguard
  wireguard:
    persistentKeepalive: 25

clusterMesh:
  enabled: true
  apiserver:
    service:
      type: LoadBalancer 

routingMode: native
# ARCHITECTURAL FIX: Define the sovereign network boundary for eBPF native routing
ipv4NativeRoutingCIDR: "172.16.0.0/16"

bpf:
  masquerade: true

ipam:
  mode: "cluster-pool"
  operator:
    clusterPoolIPv4PodCIDRList: ["172.16.0.0/16"]
    clusterPoolIPv4MaskSize: 24
