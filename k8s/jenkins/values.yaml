controller:
  image:
    tag: "2.492.1-lts-jdk21"

  # The Absolute Identity Lock
  jenkinsUrl: "https://jenkins.sikiru.co.uk/"

  javaOpts: "-Xms1024m -Xmx1024m -Djava.awt.headless=true"

  securityContext:
    runAsUser: 1000
    fsGroup: 1000

  # Probe Immunity Shield
  probes:
    startupProbe:
      failureThreshold: 60
      periodSeconds: 10

  # CPU & Memory Resources
  resources:
    requests:
      cpu: "500m"
      memory: "1Gi"
    limits:
      cpu: "1000m"
      memory: "2Gi"

  admin:
    existingSecret: "jenkins-admin-credentials"
    userKey: "jenkins-admin-user"
    passwordkey: "jenkins-admin-password"

  # The Modern Plugin Stack (Notice 'oic-auth' is present, 'keycloak' is gone)
  installPlugins:
    - workflow-aggregator
    - git
    - github-branch-source
    - job-dsl
    - kubernetes
    - credentials-binding
    - oic-auth
    - role-strategy
    - configuration-as-code

  # Vault-Backed Secret Injection
  containerEnv:
    - name: GITHUB_USER
      valueFrom:
        secretKeyRef:
          name: jenkins-github-creds
          key: username
    - name: GITHUB_TOKEN
      valueFrom:
        secretKeyRef:
          name: jenkins-github-creds
          key: token
    - name: OIDC_CLIENT_SECRET
      valueFrom:
        secretKeyRef:
          name: jenkins-keycloak-secret
          key: client-secret

  # Jenkins Configuration as Code (JCasC)
  JCasC:
    defaultConfig: true
    configScripts:
      welcome-message: |
        jenkins:
          systemMessage: "Welcome to Sikiru-Lab Jenkins CI/CD. Authenticated via Keycloak OIDC."

      credentials: |
        credentials:
          system:
            domainCredentials:
              - credentials:
                  - usernamePassword:
                      scope: GLOBAL
                      id: "github-pat"
                      username: "${GITHUB_USER}"
                      password: "${GITHUB_TOKEN}"
                      description: "GitHub PAT for GitOps Write-Back Loop"

      # The Modern OIDC Security Realm
      security: |
        jenkins:
          securityRealm:
            oic:
              clientId: "jenkins"
              clientSecret: "${OIDC_CLIENT_SECRET}"
              wellKnownOpenIDConfigurationUrl: "https://sso.sikiru.co.uk/realms/Sikiru-Lab/.well-known/openid-configuration"
              scopes: "openid profile email"
              userNameField: "preferred_username"
              groupsFieldName: "groups"
          authorizationStrategy:
            roleBased:
              roles:
                global:
                  - name: "admin"
                    description: "GridOps Platform Administrators"
                    permissions:
                      - "Overall/Administer"
                    entries:
                      - group: "jenkins-admins"
                  - name: "developer"
                    description: "GridOps Developers"
                    permissions:
                      - "Overall/Read"
                      - "Job/Build"
                      - "Job/Read"
                      - "Job/Workspace"
                    entries:
                      - group: "GridEngineers"

  # Networking (Ingress)
  ingress:
    enabled: true
    ingressClassName: nginx
    hostName: jenkins.sikiru.co.uk
    annotations:
      cert-manager.io/cluster-issuer: letsencrypt-cloudflare
      nginx.ingress.kubernetes.io/proxy-body-size: "50m"
      nginx.ingress.kubernetes.io/backend-protocol: HTTP
      # Strict 301 Loop Prevention
      nginx.ingress.kubernetes.io/force-ssl-redirect: "false"
      nginx.ingress.kubernetes.io/ssl-redirect: "false"
      # JWT Buffer Expansion
      nginx.ingress.kubernetes.io/proxy-buffer-size: "128k"
      nginx.ingress.kubernetes.io/proxy-buffers-number: "4"
      nginx.ingress.kubernetes.io/proxy-busy-buffers-size: "256k"
      nginx.ingress.kubernetes.io/large-client-header-buffers: "4 128k"
    tls:
      - secretName: jenkins-tls-cert
        hosts:
          - jenkins.sikiru.co.uk

  # Service Account for CI/CD Pipelines
  serviceAccount:
    create: true
    name: jenkins
